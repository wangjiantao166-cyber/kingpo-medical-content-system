# Secrets and Access Policy

Do not store passwords, SSH keys, API keys, database credentials, customer personal data, or standard full text in Git.

Use:

- GitHub Environments;
- GitHub Actions secrets;
- credentials references in schemas;
- dedicated deploy users;
- least privilege permissions.
